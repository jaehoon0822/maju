import React from "react";
import { CloseButton } from "../../Atoms/CloseButton";

const ModalTop = () => {
  return (
    <div>
      <CloseButton onClick={() => {}} />
    </div>
  );
};

export default ModalTop;
